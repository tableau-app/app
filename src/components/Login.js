import React from 'react';
import OAuth from './OAuth';
import Auth from './Auth';

export const Login = () => (
  <div>
    <div>
      <OAuth />
    </div>
    <div>
      <Auth/>
    </div>
  </div>
);